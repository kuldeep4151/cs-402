#ifndef calculateMean_H
#define calculateMean_H

float Mean(const float *data, int valueCount);
float GeometricMean(const float *data, int valueCount);
float HarmonicMean(const float *data, int valueCount);

#endif